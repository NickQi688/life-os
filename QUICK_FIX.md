# Life-OS 显示 0 数据 - 快速修复指南

## 最简单的诊断方法（不需要粘贴代码）

### 第一步：检查配置是否保存

1. **打开 Life-OS 网站**
2. **按 F12 打开开发者工具**
3. **切换到 Application 标签**（有些浏览器叫 "存储"）
4. **左侧找到 "Local Storage"** → 点击你的网站域名
5. **查看是否有 `lifeos_github_config` 这一项**

**如果没有这个项**：
→ 配置没保存，需要重新配置

**如果有这个项**：
→ 点击它，查看值是否包含 `token` 和 `repo`

---

### 第二步：创建测试记录

如果配置存在，但显示 0 数据，最可能的原因是**仓库是空的**。

**解决方法：**
1. 在 Life-OS 网站上点击右下角的 `+` 按钮
2. 输入 "测试记录" 或任何内容
3. 点击发送
4. **刷新页面**（Ctrl+R 或 Cmd+R）
5. 查看是否显示 1 条记录

---

### 第三步：检查 GitHub 仓库

1. 访问你的 GitHub 仓库（比如 `github.com/你的用户名/你的仓库名`）
2. 查看是否有以下文件夹：
   - `01-碎片想法`
   - `02-待办任务`
   - `03-知识库`
   - `04-日记`

**如果仓库是空的**：
→ 正常！在 Life-OS 创建记录后会自动创建这些文件夹

**如果有文件夹但里面没有文件**：
→ 在 Life-OS 中创建一条新记录

---

## 常见问题

### Q: 我配置了 GitHub，但刷新后配置消失

**原因：** 浏览器隐私模式或设置阻止了 localStorage

**解决：**
1. 退出无痕/隐私模式
2. 检查浏览器设置是否允许网站使用 localStorage
3. 暂时禁用广告屏蔽器或隐私扩展再试

---

### Q: 点击"测试连接"显示成功，但主页面还是 0 数据

**原因：** 可能是代码缓存问题

**解决：**
1. 硬刷新页面：`Ctrl + Shift + R`（Windows）或 `Cmd + Shift + R`（Mac）
2. 或清除浏览器缓存后重试

---

### Q: GitHub Token 报错 403 或 401

**解决：**
1. 访问 https://github.com/settings/tokens
2. 点击 "Generate new token (classic)"
3. **必须勾选 `repo` 权限**（完整仓库访问）
4. 生成后在 Life-OS 设置中更新

---

## 与 Obsidian 同步

Life-OS 创建的文件会直接保存在你的 GitHub 仓库中，要在 Obsidian 中查看：

**方法 1：使用 Git 同步**
1. 在本地克隆你的 GitHub 仓库
2. 在 Obsidian 中打开这个仓库作为 vault
3. 定期运行 `git pull` 同步更新

**方法 2：使用 Obsidian Git 插件**
1. 在 Obsidian 中安装 "Obsidian Git" 插件
2. 配置自动拉取间隔（如 5 分钟）

---

## 需要更多帮助？

如果以上方法都无法解决，请告诉我：
1. Local Storage 中有 `lifeos_github_config` 吗？
2. 仓库中有文件吗？
3. 浏览器控制台有什么错误信息？
