# Life-OS 与 Obsidian 集成配置指南

## 推荐方案：使用专用 lifeos 目录

### 目录结构

```
my-note/
├── lifeos/                    # Life-OS 专用目录
│   ├── 01-碎片想法/           # 灵感类型
│   ├── 02-待办任务/
│   │   ├── 今日/              # 截止日期为今天
│   │   ├── 本周/              # 截止日期在本周
│   │   ├── 本月/              # 截止日期在本月
│   │   └── 长期/              # 无截止日期或长期任务
│   ├── 03-知识库/            # 笔记类型
│   └── 04-日记/              # 日记类型
│       └── 2026/
│           └── 02/
├── TELOS/                    # 你的核心体系
├── My-Digital-Self/            # 数字分身
├── 项目复盘/                  # 深度笔记
└── ...（其他目录）
```

### 优势

1. **分类清晰**：快速捕获 vs 深度思考
   - `lifeos/` = 快速记录（收件箱）
   - `TELOS/` = 系统化内容
   - `项目复盘/` = 深度复盘

2. **工作流顺畅**：
   - 在 Life-OS 快速捕获 → 保存在 `lifeos/`
   - 定期整理有价值内容 → 移动到 `TELOS/` 或 `项目复盘/`
   - 保持 `lifeos/` 轻量，作为"流动的收件箱"

3. **互不干扰**：Life-OS 文件不会污染你的核心知识体系

---

## Life-OS 网站配置

### 配置步骤

1. **打开 Life-OS 网站**
2. **进入设置**（点击右上角设置图标）
3. **填写以下信息**：

```
GitHub Token:   [你的 Personal Access Token]
仓库名称:      a2222/my-note
分支:          main
basePath:      lifeos          ← 关键！填写这个
```

4. **点击"测试 GitHub 连接"**
5. **确认看到记录数**（应该显示 20+ 条）

---

## basePath 参数说明

| basePath 值 | 文件保存位置 | 适用场景 |
|-------------|-------------|---------|
| `""` (空) | `my-note/01-碎片想法/`<br>`my-note/03-知识库/` | 希望文件直接在根目录 |
| `"lifeos"` | `my-note/lifeos/01-碎片想法/`<br>`my-note/lifeos/03-知识库/` | ✅ **推荐**：专用目录 |

---

## 验证配置

### 方法 1：在 Life-OS 网站查看

配置保存后，网站首页应该显示已有记录（lifeos 目录中的 20+ 条）。

### 方法 2：在 GitHub 查看文件

访问你的仓库，应该看到：
```
a2222/my-note/
└── lifeos/
    ├── 01-碎片想法/
    ├── 02-待办任务/
    ├── 03-知识库/
    └── 04-日记/
```

### 方法 3：在 Obsidian 中查看

1. **同步仓库**：
   ```bash
   cd ~/Documents/qukuaiqiji/my-note
   git pull
   ```

2. **在 Obsidian 中打开**：
   - 文件应该出现在 `lifeos/` 目录下

---

## 日常使用工作流

### 快速捕获（Life-OS）

1. 在 Life-OS 网站快速记录灵感/任务
2. 内容自动保存到 `lifeos/01-碎片想法/` 或 `lifeos/02-待办任务/`
3. Git 同步到 Obsidian

### 定期整理（Obsidian）

每周/每月整理一次：
1. 在 Obsidian 中打开 `lifeos/` 目录
2. 将有价值的内容移动到对应的核心目录：
   - 深度思考 → `TELOS/`
   - 项目经验 → `项目复盘/`
   - 学习笔记 → `学习/` 或 `03-知识库/`（根目录的）
3. 删除已处理的 transient 记录

---

## Git 自动同步脚本

### 方法 1：定时同步（cron）

```bash
# 编辑 crontab
crontab -e

# 每 10 分钟拉取一次
*/10 * * * * cd ~/Documents/qukuaiqiji/my-note && git pull
```

### 方法 2：Obsidian Git 插件

1. 在 Obsidian 中安装 "Obsidian Git" 插件
2. 配置：
   - 自动拉取间隔：10 分钟
   - 自动提交间隔：5 分钟
3. 启用插件

---

## 目录清理建议

### 保持 lifeos 目录轻量

`lifeos/` 作为"收件箱"，建议：
- ✅ 保留：尚未处理的新想法
- ✅ 保留：待办任务（进行中）
- ❌ 移出：已完成的深度复盘 → 到 `项目复盘/`
- ❌ 移出：已学习的系统性笔记 → 到 `TELOS/` 或 `学习/`

### 清理时机

- **每日**：清理已完成任务
- **每周**：整理有价值内容到核心体系
- **每月**：归档旧记录到 `lifeos/04-日记/` 按年月组织

---

## 常见问题

### Q: 为什么要用 lifeos 子目录而不是直接用根目录？

**A:**
- 根目录已经有很多系统化内容（TELOS、项目复盘等）
- Life-OS 快速捕获的内容质量不一，需要筛选
- 专用目录便于批量整理和归档

### Q: 已经在根目录有 01-碎片想法 等目录，怎么办？

**A:** 两种方案：

**方案 A - 迁移到 lifeos（推荐）：**
```bash
# 移动现有内容到 lifeos 目录
mv "01-碎片想法"/* lifeos/01-碎片想法/
mv "03-知识库"/* lifeos/03-知识库/
rm -rf "01-碎片想法" "02-待办任务" "03-知识库"
```

**方案 B - 保持现状：**
- 配置 `basePath: ""`
- 文件直接保存在根目录
- 缺点：与现有目录可能冲突

### Q: 如何配置才能让两个地方都正常工作？

**A:** 推荐使用 `basePath: "lifeos"`，理由：
1. Life-OS 作为"快速输入工具"
2. Obsidian 作为"深度整理工具"
3. 定期从 lifeos → 核心目录移动有价值内容

---

## 配置总结

**在 Life-OS 网站设置中填写：**

```
GitHub Token:   ghp_xxxxxxxxxxxxxxxxxxxxx
仓库名称:      a2222/my-note
分支:          main
basePath:      lifeos
```

**配置完成后的验证：**
1. ✅ Life-OS 网站显示 20+ 条记录
2. ✅ 创建新记录后，文件出现在 `lifeos/` 对应目录
3. ✅ Obsidian 中 git pull 后能看到新文件
